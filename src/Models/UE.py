class UE:
    def __init__(self, ue_code: str, title_fr: str, ects: str, note: str, avg_note: str, rank: str, resultat: str):
        self.ue_code = ue_code
        self.title_fr = title_fr
        self.ects = ects
        self.note = note
        self.avg_note = avg_note
        self.rank = rank
        self.resultat = resultat